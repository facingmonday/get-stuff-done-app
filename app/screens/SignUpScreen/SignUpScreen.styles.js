import { StyleSheet } from 'react-native';
import { COLORS } from '../../theme';

export default StyleSheet.create({
  goBackContainer: {
    alignItems: 'center',
    marginTop: 10,
  },
  backText: {
    color: COLORS.WHITE,
  },
});
