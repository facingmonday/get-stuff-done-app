import React from 'react';
import { View, Text } from 'react-native';
import styles from './TabBar.styles';

function MenuButtonScreen() {
  return (
    <View style={styles.menuButtonScreen}>
      <Text>MenuButton Screen</Text>
    </View>
  );
}

export default MenuButtonScreen;
