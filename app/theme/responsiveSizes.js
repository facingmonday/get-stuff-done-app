import {
  responsiveFontSize,
  responsiveHeight,
  responsiveWidth,
} from 'react-native-responsive-dimensions';

export const RH = responsiveHeight(1.5);
export const RW = responsiveWidth(2.67);
export const RFS = responsiveFontSize(1.31);

export default {
  RH, RW, RFS,
};
